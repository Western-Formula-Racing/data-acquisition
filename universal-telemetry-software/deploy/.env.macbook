# MacBook base station (dev/test)
# Usage: docker compose -f deploy/docker-compose.macbook.yml --profile base up -d
# Note: REMOTE_IP is hardcoded in docker-compose.macbook.yml (10.71.1.10)
INFLUX_SEASON=WFR26-test
LOCAL_INFLUX_TOKEN=apiv3_local-telemetry-token
GRAFANA_ADMIN_PASSWORD=admin
DBC_HOST_PATH=../secret-dbc/WFR25.dbc
