# Base Station RPi (.20)
# Usage: docker compose -f deploy/docker-compose.prod.yml --profile base up -d
REMOTE_IP=192.168.1.10
INFLUX_SEASON=WFR26
LOCAL_INFLUX_TOKEN=apiv3_local-telemetry-token
GRAFANA_ADMIN_PASSWORD=admin
SET_TIME_ENABLED=true
CLOUD_INFLUX_URL=https://influxdb3.westernformularacing.org
CLOUD_INFLUX_TOKEN=
